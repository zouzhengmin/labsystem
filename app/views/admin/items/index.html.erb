<div>
  <div class="pull-right">
    <%= link_to "添加检测项目", new_admin_item_path, class: "btn btn-primary" %>
  </div>
  <h1>
    样品(<%= @items.count %>)
  </h1>
</div>

<div>
  <table class="table table-striped">
    <tr>
      <td>序号</td>
      <td>测试项目</td>
    </tr>
    <% @items.each_with_index do |item, index| %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= item.name %></td>
      </tr>
    <% end %>
  </table>
</div>
<br>
<%= link_to "添加样品", new_sample_path, class: "btn btn-primary" %>
