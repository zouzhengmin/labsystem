<h1>测试项目</h1>
<hr>

<table>
  <tr>
    <td>序号</td>
    <td>测试项目</td>
  </tr>
  <% @items.each_with_index do |item, index| %>
    <tr>
      <td><%= index + 1 %></td>
      <td><%= item.name %></td>
      <td><%= link_to "编辑", edit_admin_item_path(item) %></td>
      <td><%= link_to "删除", admin_item_path(item), method: :delete %></td>
    </tr>
  <% end %>
  <tr>
    <td><%= link_to "添加", new_admin_item_path %></td>
  </tr>

</table>
<br>
<%= link_to "添加样品", new_sample_path %>
