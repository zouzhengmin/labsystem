<table>
  <%= form_for @sample do |f| %>
  <tr>
    <td>产品名称：</td>
    <td><%= f.text_field :name %></td>
  </tr>
  <tr>
    <td>样品编码：</td>
    <td><%= f.text_field :code %></td>
  </tr>

  <tr>
    <td><%= f.submit "提交" %></td>
  </tr>
  <% end %>
</table>
